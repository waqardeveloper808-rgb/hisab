# Product / Item / Service System — `fail` list (control point engine, this repo)

**Stated UI:** Fail = 1.  
**Engine result (`moduleId: "product-item-service"`):** **0** `fail` (6 CPs, all `pass` in this run).

**Frozen fail IDs:** *none* (empty set).

**Validation vs stakeholder (1):** **MISMATCH**.
