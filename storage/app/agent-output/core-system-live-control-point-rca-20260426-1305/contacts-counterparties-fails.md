# Contacts & Counterparties — `fail` list (control point engine, this repo)

**Stated UI:** Fail = 8.  
**Engine result (`moduleId: "contacts-counterparties"`):** **0** `fail` (all CPs in this subcategory are `pass` in this run).

**Frozen fail IDs:** *none* (empty set).

**Validation vs stakeholder (8):** **MISMATCH**.
