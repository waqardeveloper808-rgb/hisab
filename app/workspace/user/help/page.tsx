import { WorkspaceHelpCenter } from "@/components/workspace/WorkspaceHelpCenter";

export const metadata = {
  title: "Workspace — Help",
};

export default function WorkspaceHelpCenterPage() {
  return <WorkspaceHelpCenter />;
}
