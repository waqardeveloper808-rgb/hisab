import { DebitNotesRegisterClient } from "./DebitNotesRegisterClient";

export const metadata = {
  title: "Workspace — Debit notes",
};

export default function WorkspaceDebitNotesPage() {
  return <DebitNotesRegisterClient />;
}
