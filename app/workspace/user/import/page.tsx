import { WorkspaceImportHub } from "@/components/workspace/WorkspaceImportHub";

export const metadata = {
  title: "Workspace — Import",
};

export default function Page() {
  return <WorkspaceImportHub />;
}
