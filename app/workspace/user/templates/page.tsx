import { WorkspaceTemplatesList } from "@/components/workspace/WorkspaceTemplatesList";

export const metadata = {
  title: "Workspace — Templates",
};

export default function WorkspaceTemplatesPage() {
  return <WorkspaceTemplatesList />;
}
