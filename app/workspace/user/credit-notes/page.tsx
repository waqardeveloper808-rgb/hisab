import { CreditNotesRegisterClient } from "./CreditNotesRegisterClient";

export const metadata = {
  title: "Workspace — Credit notes",
};

export default function WorkspaceCreditNotesPage() {
  return <CreditNotesRegisterClient />;
}
